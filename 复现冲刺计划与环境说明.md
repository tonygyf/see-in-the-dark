# See in the Dark 4周冲刺计划与环境说明

## 1. 当前环境状态（已完成）
- 项目目录：`d:\typer\cursor project\see in the dark`
- Python版本：`3.10.11`
- 已创建虚拟环境：`.venv`
- 已安装核心依赖：`PyTorch + 数据处理 + 几何后处理 + 可视化`
- 当前PyTorch：`torch 2.11.0+cpu`（`torch.cuda.is_available() == False`）

## 2. 一键进入环境
在 PowerShell 中执行：

```powershell
cd "d:\typer\cursor project\see in the dark"
.\.venv\Scripts\Activate.ps1
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available())"
```

## 3. 依赖文件
- 依赖清单：`requirements.txt`
- 如需重装（CPU版PyTorch）：

```powershell
cd "d:\typer\cursor project\see in the dark"
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

## 4. 4周冲刺版（复现+学习并行）
### 第1周：论文拆解 + 基线脚手架
- 目标：把论文转成可编码任务，跑通最小训练流程。
- 复现任务：
  - 拆解论文模块：`SCM / DSF / TSR / Loss`
  - 设计代码结构：`src/datasets`、`src/models`、`src/train.py`、`src/eval.py`
  - 跑通一个最小样例（随机数据也可）验证训练循环。
- 学习任务（每天）：
  - Python：函数、类、文件组织、异常处理
  - PyTorch：`Tensor`、`Dataset/DataLoader`、`forward/backward`
- 周交付：
  - 一份模块流程图
  - 一版可运行训练脚手架

### 第2周：复现基线检测器
- 目标：先得到不含 `SCM/DSF/TSR` 的基线结果。
- 复现任务：
  - 搭建主干网络和检测头
  - 实现基础损失（如分割/回归项）
  - 打通评估流程与可视化输出
- 学习任务（每天）：
  - 深度学习：卷积、特征图、损失函数、优化器
  - PyTorch：`nn.Module`、参数更新、学习率调度
- 周交付：
  - 基线模型可训练
  - 首轮指标与失败案例截图

### 第3周：加入SCM与空间约束
- 目标：实现论文关键改进之一 `SCM + Lsr/Lss`。
- 复现任务：
  - 在训练阶段接入辅助模块SCM
  - 实现 `Lsr`（重建约束）与 `Lss`（语义约束）
  - 对比实验：`Baseline` vs `Baseline+SCM`
- 学习任务（每天）：
  - 多分支网络与辅助监督
  - 特征对齐、上采样、mask监督
- 周交付：
  - 消融表（至少一组）
  - 中间特征可视化（文本区域热力图）

### 第4周：加入DSF+TSR并完成报告
- 目标：拼齐完整方法并整理复现结论。
- 复现任务：
  - 加入 `DSF`（动态蛇形特征建模）
  - 加入 `TSR`（旋转矩形累积塑形）
  - 完成总损失：`Lt = Lseg + LH + Lθ + Lss + Lsr`
  - 输出最终指标、案例分析、与论文差异说明
- 学习任务（每天）：
  - 注意力机制、几何参数回归 `(x, y, h, w, θ)`
  - 后处理与形态学操作思想
- 周交付：
  - 最终复现报告（含指标/可视化/误差分析）

## 5. 每日执行模板（建议2~3小时）
- `30分钟`：学习当天必需知识点（只学本次要用的）
- `90分钟`：编码与调试（单模块）
- `20分钟`：记录笔记（输入张量、输出张量、loss变化、问题）
- `10分钟`：计划次日任务（一个可验证目标）

## 6. 当前目录建议结构
```text
see in the dark/
  ├─ .venv/
  ├─ checkpoints/
  ├─ data/
  ├─ docs/
  ├─ runs/
  ├─ src/
  ├─ requirements.txt
  ├─ 复现冲刺计划与环境说明.md
  └─ 许程沛论文.pdf
```

## 7. 下一步（明天即可执行）
- 步骤1：在 `src/` 下创建最小训练脚手架（我可以下一条消息直接帮你生成）。
- 步骤2：先用假数据跑通1个epoch，确认日志/保存/评估流程。
- 步骤3：再接入真实数据读取与标注解析。
